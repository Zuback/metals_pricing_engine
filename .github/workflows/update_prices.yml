name: Run Pricing Engine

on:
  schedule:
    - cron: '0 */4 * * *' # Runs every 4 hours
  workflow_dispatch:

# Prevent two pricing jobs from modifying main at the same time.
concurrency:
  group: pricing-engine
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Setup Dart
        uses: dart-lang/setup-dart@v1

      - name: Install Dependencies
        run: dart pub get

      - name: Run Pricing Engine
        run: dart run bin/metals_pricing_engine.dart

      - name: Commit and Push Updated prices.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add prices.json

          if git diff --staged --quiet; then
            echo "No changes to prices.json to commit."
            exit 0
          fi

          git commit -m "Auto-update prices.json [skip ci]"

          # Make sure we're based on the latest main before pushing.
          git fetch origin main
          git rebase origin/main

          git push origin HEAD:main